// Crea una función que devuelva el número PI con dos decimales. Utiliza la
// variable PI que ya existe en JavaScript.

const pi2Decimales = () => {
    console.log(Math.PI.toFixed(2));
}

pi2Decimales();